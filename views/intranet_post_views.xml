<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_intranet_post_tree" model="ir.ui.view">
        <field name="name">intranet.post.tree</field>
        <field name="model">intranet.post</field>
        <field name="arch" type="xml">
            <tree string="Publications">
                <field name="name"/>
                <field name="author_id"/>
                <field name="department_id" optional="show"/>
                <field name="create_date"/>
            </tree>
        </field>
    </record>

    <record id="view_intranet_post_form" model="ir.ui.view">
        <field name="name">intranet.post.form</field>
        <field name="model">intranet.post</field>
        <field name="arch" type="xml">
            <form string="Publication">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="author_id" readonly="1"/>
                        <field name="department_id"/>
                        <field name="post_type"/>
                        <field name="active"/>
                    </group>
                    <group>
                        <field name="body" colspan="4" nolabel="1" widget="html"/>
                    </group>
                    <group>
                        <field name="image" widget="image" class="oe_avatar" alt="Image du post"/>
                        <field name="attachment_ids" widget="many2many_binary"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_intranet_post_kanban" model="ir.ui.view">
        <field name="name">intranet.post.kanban</field>
        <field name="model">intranet.post</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_small_column">
                <field name="image"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click">
                            <t t-if="record.image.raw_value">
                                <img t-att-src="'/web/image/intranet.post/' + record.id + '/image'" class="o_mini_avatar" alt="Image du post"/>
                            </t>
                            <div class="oe_kanban_details">
                                <strong>
                                    <field name="name"/>
                                </strong>
                                <div t-if="record.body.raw_value">
                                    <t t-esc="record.body.raw_value[:100] + '...'" />
                                </div>
                                <div class="oe_subtle">
                                    <field name="create_date"/>
                                </div>
                            </div>

                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
    <record id="action_intranet_post" model="ir.actions.act_window">
        <field name="name">Posts</field>
        <field name="res_model">intranet.post</field>
        <field name="view_mode">kanban,tree,form</field>
    </record>
</odoo>
